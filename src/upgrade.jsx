import './upgrade.css'

function Upgrade({upgradeInfo, CookieNum}){
    
    return(
        <div id="upgrade-container">
                {upgradeInfo.name}
        </div>
    );

}

export default Upgrade; 